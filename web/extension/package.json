{
  "name": "ri-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Relationship Intelligence Chrome extension — LinkedIn + Facebook social monitoring.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
